export default function NotFound() {
  return (
    <main className="error-panel">
      <div className="error-card">
        <p>Open a shared story link to play — for example /s/yourcode</p>
      </div>
    </main>
  );
}
